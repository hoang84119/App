import React, { Component } from 'react';
import { Text } from 'react-native';
class Account extends Component {
    render() {
        return (
            <Text>Màn hình Account</Text>
        );
    }
}

export default Account;